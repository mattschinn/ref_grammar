---
title: Diacritic Typer
description: Convert plain ASCII postfix triggers into the conlang's diacritics and IPA, with room for longer text.
---

import DiacriticTyper from '../../../components/DiacriticTyper.astro';

The conlang's orthography leans on diacritics that are awkward to type directly.
Type the **postfix triggers** below (the base letter first, then the trigger) and
the converted form appears live — the same engine that powers the search box, but
with room for longer passages, an IPA keypad, and styling controls.

<DiacriticTyper />
